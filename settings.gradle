pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

// Configure dependency resolution management
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'leechtext-java'
